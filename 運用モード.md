# 運用モード

このプロジェクトの操作モードの定義。本多が合図を出すと、モード選択ウインドウを表示し、選ばれたモードで作業する。

## 合図（トリガー）

- **`start`**（記号なしの語）… これを入力したら、モード選択ウインドウ（選択肢）を表示する。実行・スタートの合図。
- **即座に出すこと。** `start` を「不明なコマンド」扱いしたり、「何をしたいですか？」と聞き返してはいけない。合図が来たら迷わず下記4モードの選択ウインドウを表示する。
- セッション冒頭で `start` が来たら、必要ならこの運用モード.mdを読んで最新のモード定義を確認したうえで、4モードを出す。
- 注意: 先頭 `/` はアプリのスキル／コマンド用に予約されているため、`/start` は使えない（横取りされる）。`/` なしの語にすること。
- 選択ウインドウには下記4モードを出し、本多に選ばせる。

## モード一覧

### 1. インプット（蓄積）

すでにある要約文＋感想文を入力し、プロジェクトを更新するモード。

- 要約文原文を `要約文原文/` に、感想文を `感想データ/` に保存（命名・書式は [ルール.md](ルール.md) 準拠）。
- 索引.md に1行追加。感想は一字一句そのまま。
- 会話・やり取りから傾向の追記候補に気づいたら提案する（常時発動ルール）。

### 2. アウトプット・フル生成

要約文だけを渡されたら、既存情報だけで感想文を生成するモード。

- 情報源は [傾向まとめ.md](傾向まとめ.md)（特に着眼点・反応パターン・文体・予測チェックリスト）と、蓄積済みの `感想データ/`。
- 感想の要点メモはもらわず、「本多ならどこに着目し、どう反応するか」を推定して出力する。

### 3. アウトプット・要点合成

要約文＋本多の「感想の要点ポイント」を渡されたら、それを核に既存情報で肉付け・仕上げるモード。

- 要点（本多の実際の着目・評価）を必ず芯に据え、傾向まとめの文体・反応パターンで文章化する。
- 要点と矛盾する肉付けはしない。要点が主、傾向は従。

### 4. メンテ／その他

傾向まとめ・読書傾向の更新、ルール編集、索引整理、著者名の修正など。

**傾向まとめの整理・剪定パス**：蓄積が増えたら、量を足すだけでなく定期的に手入れする。(1) 重複・冗長の統合、(2) 一回きりの反応と安定した傾向の区別（怪しいものは根拠の薄さを明示 or 保留）、(3) 古くなった・矛盾する記述の削除・修正、(4) 重みづけタグ（【中核】／【反復】／【一回性・仮】）の見直し——根拠#数の増減や新規事例に応じて昇格・降格し、剪定と連動させる。締まった短いドキュメントの方が生成の精度が上がる（§6「無理に明示化すると却って痩せる」と同趣旨）。フィードバック由来の記述（生成が外した差分）は精度への寄与が大きいので優先して残す。なお傾向を新規追記するときは、その場で重みづけタグも付ける（タグ運用の定義は傾向まとめ.mdヘッダ）。

**版管理（git）**：プロジェクトはgit管理。傾向まとめを含め確定変更のたびにコミットし、節目にはタグ（v0, v1, …）を付ける。差分は `git diff <rev> <rev>` で追える（本多に頼まれたら提示）。本体 `傾向まとめ.md` が常に最新版。各ドキュメントの最終更新日・変遷はgit（コミット履歴）に委ね、本文には持たない。傾向まとめの節目・剪定の意図だけは例外的に 傾向まとめ.md の更新ログにも人が読める形で残す。

## アウトプット共通ルール（モード2・3）

- **まずチャットに表示する。** いきなりファイル保存はしない。
- その後のやり取りで本多が確定させたら、感想文の md ファイルとして保存する。
- 保存する生成物は、本物の感想（本人が一字一句書いたもの）と区別するため、メタ情報に種別を明記する。
  - 本人記述（verbatim）: `- 種別: 本人記述`
  - 生成→本多確認済み: `- 種別: 生成（本多確認済み）`
- **トレーサビリティ**：生成した感想の末尾に、根拠として使った傾向まとめの節番号を添える（例: 「根拠: 傾向まとめ §2, §3, §5」）。モデル化のズレを本多が見つけやすくするため。
- **フィードバック連結**：本多が出力に直しを入れたら、その差分は貴重なシグナルなので、傾向まとめへの追記を提案する（常時発動ルール）。
- **長さ・構成**：感想は簡潔に（本人記述の実態は短い段落2〜4本。#022 #027 #028 #029 #030）。入り方は2通りあり、本の内容を1文で要約して入る型と、所感・自分の事情・情の表明・不満から入る型がある（実績はおおむね半分ずつ。詳細は 傾向まとめ.md §5）。どちらで入るかは題材と本多の要点次第で、概要1文は必須ではない。冗長な生成は避ける。**なお概要から入る場合も、それは内容の流れの話であって、`概要:`／`所感:` のようなラベル・見出しを付ける意味ではない（下項参照）。**
- **体裁は本多の地の文で（勝手に整形しない）**：生成する感想は本多が実際に書くような地の文で書く。`概要:`／`所感:` などのラベルや見出しを勝手に付けず、Claude 自身の出力癖（見出し化・太字ラベルでの整理など）を本多の感想文に持ち込まない。感想データは本多の文体の再現が目的であり、体裁を足すと再現性が損なわれる。ただし本多から書き方の明示指示があった場合は、その指示に従って変更してよい。
- **語彙は本人記述に寄せる（Claude の口癖を混ぜない）**：生成する感想の語彙・言い回しは、本人記述の感想データ（corpus）に実在する本多の語彙に寄せる。Claude が使いがちで本多の本人記述に出てこない語（例:「刺さる／刺さった」）を混ぜない。ある語を使う前に必ず本人記述を検索して使用実績を確認する（例: `grep` で該当語を検索）。「迷ったら」ではなく、主張・使用の前に必ず検証する（指摘されてから検証するのでは遅い）。上の「体裁は本多の地の文で」と同系統の再発防止で、本多から明示指示がある場合を除く。会話で本多の語彙・文体を語る場合も同じ（CLAUDE.md 常時ルール）。
- **鉤括弧**：原文（著者・ライター）が実際に使っている表現にのみ「」を付け、本多の言い換えには付けない（ルール.md準拠）。

## 種別メタ情報の運用

- 今後 `感想データ/` に保存する感想は、メタ情報に `- 種別:` を付ける。
- 既存の蓄積分（本人が貼り付けた感想）は「本人記述」扱い。`- 種別:` の記載がないものは本人記述とみなす。
